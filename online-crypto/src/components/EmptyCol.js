import React, { Component } from 'react';
import { Col } from 'react-materialize';

class EmptyCol extends Component {
  render() {
    return (
      <Col s={1} />
    );
  }
}

export default EmptyCol;